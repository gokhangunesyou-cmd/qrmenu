{% extends 'admin/base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}
<div class="row g-4">
    <div class="col-lg-7">
        <div class="card h-100">
            <div class="card-header d-flex justify-content-between align-items-center">
                <div>
                    <h4 class="mb-1">Genel Bakış</h4>
                    <small class="text-muted">MVP için hızlı yönetim alanı</small>
                </div>
                <span class="badge bg-primary">Canlı</span>
            </div>
            <div class="card-body">
                <div class="row g-3">
                    <div class="col-md-6">
                        <div class="p-3 rounded-4 bg-light border">
                            <div class="text-muted small">Ürün Yönetimi</div>
                            <div class="fw-semibold mt-1">Menüyü güncelle</div>
                            <a href="{{ path('admin_product_web_index') }}" class="btn btn-sm btn-primary mt-3">Ürünlere git</a>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="p-3 rounded-4 bg-light border">
                            <div class="text-muted small">QR Kodları</div>
                            <div class="fw-semibold mt-1">Masa kodlarını yönet</div>
                            <a href="{{ path('admin_qrcode_index') }}" class="btn btn-sm btn-primary mt-3">QR alanı</a>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="p-3 rounded-4 bg-light border">
                            <div class="text-muted small">Medya Kütüphanesi</div>
                            <div class="fw-semibold mt-1">Görsel arşivi</div>
                            <a href="{{ path('admin_media_index') }}" class="btn btn-sm btn-primary mt-3">Medya</a>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="p-3 rounded-4 bg-light border">
                            <div class="text-muted small">Restoran Profili</div>
                            <div class="fw-semibold mt-1">Bilgileri düzenle</div>
                            <a href="{{ path('admin_restaurant_show') }}" class="btn btn-sm btn-outline-secondary mt-3">Profili gör</a>
                        </div>
                    </div>
                </div>
                <div class="mt-4 p-4 rounded-4" style="background: linear-gradient(135deg, rgba(37,99,235,0.12), rgba(249,115,22,0.12));">
                    <div class="fw-semibold">Hızlı not</div>
                    <div class="text-muted small">MVP aşamasında temel akışlar burada. Eksikleri birlikte kapatıyoruz.</div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-lg-5">
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0">Hızlı İşlemler</h5>
            </div>
            <div class="card-body d-grid gap-2">
                <a href="{{ path('admin_product_web_create') }}" class="btn btn-primary">
                    <i class="bi bi-plus-lg"></i> Yeni Ürün Ekle
                </a>
                <a href="{{ path('admin_media_index') }}" class="btn btn-outline-secondary">
                    <i class="bi bi-cloud-upload"></i> Medya Yükle
                </a>
                <a href="{{ path('admin_restaurant_edit') }}" class="btn btn-outline-secondary">
                    <i class="bi bi-pencil-square"></i> Restoran Düzenle
                </a>
            </div>
        </div>
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">Durum</h5>
            </div>
            <div class="card-body">
                <div class="d-flex align-items-center justify-content-between">
                    <div>
                        <div class="text-muted small">Oturum</div>
                        <div class="fw-semibold">Aktif</div>
                    </div>
                    <span class="badge bg-success">OK</span>
                </div>
                <hr>
                <div class="text-muted small">Destek için notlarınızı paylaşabilirsiniz.</div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
