{% extends 'admin/base.html.twig' %}

{% block title %}Kategori Düzenle: {{ category.name }}{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>Kategori Düzenle: <span class="text-primary">{{ category.name }}</span></h2>
        <a href="{{ path('admin_category_index') }}" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left"></i> Listeye Dön
        </a>
    </div>

    {% include 'admin/category/_form.html.twig' with {'form': form, 'category': category} %}
</div>
{% endblock %}
