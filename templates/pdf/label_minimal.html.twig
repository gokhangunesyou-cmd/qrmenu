<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Helvetica, Arial, sans-serif; margin: 0; padding: 20px; }
        .grid { display: flex; flex-wrap: wrap; gap: 10px; }
        .label {
            width: 30%;
            padding: 15px;
            text-align: center;
            page-break-inside: avoid;
        }
        .qr-image { width: 100px; height: 100px; margin: 0 auto; }
        .table-number { font-size: 16px; font-weight: bold; margin-top: 8px; color: #000; }
        .table-label { font-size: 9px; color: #666; }
    </style>
</head>
<body>
    <div class="grid">
        {% for qr in qrCodes %}
        <div class="label">
            <div class="qr-image">
                <img src="{{ qr.encodedUrl }}" alt="QR" style="width:100px;height:100px;">
            </div>
            {% if qr.tableNumber %}
                <div class="table-number">{{ qr.tableNumber }}</div>
            {% endif %}
            {% if qr.tableLabel %}
                <div class="table-label">{{ qr.tableLabel }}</div>
            {% endif %}
        </div>
        {% endfor %}
    </div>
</body>
</html>
