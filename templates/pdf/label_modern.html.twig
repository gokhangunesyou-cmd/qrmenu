<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Helvetica, Arial, sans-serif; margin: 0; padding: 15px; background: #f5f5f5; }
        .grid { display: flex; flex-wrap: wrap; gap: 15px; }
        .label {
            width: 45%;
            background: #fff;
            border-radius: 16px;
            padding: 24px;
            text-align: center;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
            page-break-inside: avoid;
        }
        .restaurant-name { font-size: 12px; text-transform: uppercase; letter-spacing: 2px; color: #999; margin-bottom: 15px; }
        .qr-image { width: 140px; height: 140px; margin: 0 auto; }
        .table-number { font-size: 28px; font-weight: 300; margin-top: 15px; color: #222; }
        .table-label { font-size: 11px; color: #888; margin-top: 4px; }
        .scan-text { font-size: 9px; color: #aaa; margin-top: 10px; text-transform: uppercase; letter-spacing: 1px; }
    </style>
</head>
<body>
    <div class="grid">
        {% for qr in qrCodes %}
        <div class="label">
            <div class="restaurant-name">{{ restaurant.name }}</div>
            <div class="qr-image">
                <img src="{{ qr.encodedUrl }}" alt="QR" style="width:140px;height:140px;">
            </div>
            {% if qr.tableNumber %}
                <div class="table-number">#{{ qr.tableNumber }}</div>
            {% endif %}
            {% if qr.tableLabel %}
                <div class="table-label">{{ qr.tableLabel }}</div>
            {% endif %}
            <div class="scan-text">Scan for menu</div>
        </div>
        {% endfor %}
    </div>
</body>
</html>
